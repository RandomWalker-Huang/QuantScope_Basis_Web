# 发布边界与安全说明

## 公开内容

`index.html` 包含浏览器展示所需的静态程序以及处理后的升贴水分析数据，包括日期、指数价格、期货价格、合约标识、到期日、剩余期限和升贴水派生序列。

## 非公开内容

以下内容必须只保留在本地 QuantScope 项目中：

- RQData账号、密码、Token及凭证指纹；
- QuantScope账号库、管理员信息与会话密钥；
- Python后台、回测引擎、策略代码和任务队列；
- 本地缓存、数据库、日志和原始全量行情文件。

## 更新要求

每次替换 `index.html` 前必须运行 `tools/validate_public_site.py`。如果检查失败，不应提交或推送该文件。

GitHub Pages 是静态公开托管。不要依赖隐藏链接、`robots.txt` 或仓库名称实现访问控制；如需限定访问人员，应使用独立的身份认证层。

